{
  "name": "my-vercel-project",
  "version": "1.0.0",
  "description": "API for managing the whitelist",
  "main": "api/whitelist.js",  // Change this to point to your main entry file if needed
  "scripts": {
    "start": "vercel dev",
    "build": "vercel build"
  },
  "dependencies": {
    "axios": "^1.5.0"  // Add any other dependencies you need here
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14"
  }
}

{
  "name": "my-vercel-project",
  "version": "1.0.0",
  "description": "API for managing the whitelist",
  "main": "api/whitelist.js",
  "scripts": {
    "start": "vercel dev",
    "build": "vercel build"
  },
  "dependencies": {
    "axios": "^1.5.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14"
  }
}
